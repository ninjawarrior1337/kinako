# WARNING: Encrypt this file with SOPS before committing.
# Run: sops --encrypt apps/immich/4-secrets.yaml > apps/immich/4-secrets.yaml
# (sops --in-place encrypts in-place)
#
# The age key for this repo is configured in .sops.yaml.
apiVersion: v1
kind: Secret
metadata:
  name: immich-secrets
type: Opaque
stringData:
  db-password: "changeme-32-char-random-password!!"
