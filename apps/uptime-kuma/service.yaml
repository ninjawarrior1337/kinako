apiVersion: v1
kind: Service
metadata:
  name: uptime-kuma
  labels:
    app: uptime-kuma
spec:
  type: LoadBalancer
  loadBalancerClass: tailscale
  ports:
    - port: 3001
      targetPort: 3001
      name: http
  selector:
    app: uptime-kuma
