apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ollama
  annotations:
    tailscale.com/proxy-group: web-proxies
spec:
  ingressClassName: tailscale
  tls:
    - hosts:
        - ollama
  defaultBackend:
    service:
      name: ollama
      port:
        number: 11434
