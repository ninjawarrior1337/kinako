apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: opencode
  annotations:
    tailscale.com/proxy-group: web-proxies
spec:
  ingressClassName: tailscale
  tls:
    - hosts:
        - opencode
  defaultBackend:
    service:
      name: opencode
      port:
        number: 4096
