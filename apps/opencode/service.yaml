apiVersion: v1
kind: Service
metadata:
  name: opencode
  labels:
    app: opencode
spec:
  type: LoadBalancer
  loadBalancerClass: tailscale
  ports:
  - port: 8080
    targetPort: 8080
    name: http
  selector:
    app: opencode
